const initState = {
  byId: {
    1: { name: "P90MisutoWolf", exercises: [1] }
  },
  allIds: [1]
};

const workouts = (state = initState, action) => {
  switch (action.type) {
    default:
      return state
  }
};

export default workouts